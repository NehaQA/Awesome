name: Cypress Test
on: push
jobs:
  cypress-run:
    runs-on: ubuntu-22.04
    steps:
      - name: Cypress run
        uses: cypress-io/github-action@v6
        with:
          command: git push origin main
          browser: chrome
